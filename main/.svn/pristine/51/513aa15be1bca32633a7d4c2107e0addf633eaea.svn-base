import React, { Suspense } from 'react'
import CreateTab from '../views/CreateTab'

const AppContent = ({setTabInstance}) => {
  return (
    <CreateTab setTabInstance={(value) => {
      setTabInstance(value);
    }} />
  )
}

export default React.memo(AppContent)